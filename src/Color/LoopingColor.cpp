#include "LoopingColor.h"

void handleLoopingColor(float &period, int &colorTime, unsigned long &currentTime, unsigned long &startTime, Color &localColor, std::vector<Color> &loopingColorList)
{
    Color color(0,0,0);
    
    if (colorTime < loopingColorList.back().riseTime)
    {

    }
}